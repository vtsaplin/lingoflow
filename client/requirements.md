## Packages
(none needed)

## Notes
The app uses the shadcn sidebar component which is already in the codebase.
Icons are from lucide-react which is already installed.
Routing is handled by wouter which is already installed.
Data fetching via TanStack Query which is already installed.
